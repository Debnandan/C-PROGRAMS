#include <stdio.h>
#include <conio.h>
int main()
{
    int l, area;
    printf("enter length");
    scanf("%d", &l);
    area = l * l;
    printf("area of square = %d", area);
    return 0;
}